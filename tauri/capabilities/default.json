{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Capabilidad del shell — habilita los APIs core de Tauri en la ventana principal de la app FEAB. La frontend no llama plugins desde JS (todos los datos viajan via FastAPI :8000), pero la capability default es requerida por Tauri 2 para arrancar la ventana.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
